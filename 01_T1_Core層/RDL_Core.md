# RDL_Core
*T1：Core層 / CORE / ACTIVE v1.1*
*依存：（T0暗黙前提）*

---

## ■ 0. 位置づけ

本文書は、関係力学言語（RDL: Relational Dynamics Language）の**中核数理層**を独立化したものである。

### 0.1 Core が扱う範囲

Core は以下を扱う：

```text
・基底量の定義（ξ / B / M_B / EFP / F / E）
・基底量間の基本式
・関係保存則
・ξ の残存性
・整合と跳躍の同一性
```

Core は以下を扱わない（外部参照）：

```text
・B 仮設の操作論              → SILN 分解（数理フレームワーク v2.2）
・B/M_B の耐久検査・残存・移植 → 構造破壊学（前提文書 v0.4）
・他理論の借用                 → 借用モジュール群（NAM / GR-MOD / FluctMOD）
・モジュール切替の判定         → RDL_切替判定 v1.0
・運用上の道具化リスク         → RDL 道具特性 v0.1
```

### 0.2 設計方針

Core は、RDL の**最小ダイナミクスと基底量**に絞る。
追従運用・破壊検査・借用判断は外部に置く。

```text
Core が扱う      ：基底量・基本式・保存則・最小ダイナミクスの骨格
Core が扱わない  ：追従運用・耐久検査・借用判断・切替判定・運用論
```

理由：

```text
・Core は最も汎用な再利用部品である
・自己検査機構を内蔵すると軽量用途で過剰になる
  （例：NPC的AIエージェントは薄い構造破壊で十分）
・用途に応じて検査の重さを選べる構造が望ましい
```

Core = 動かす部品。構造破壊学 = 検査する部品。両者の分離が本文書の前提である。

---

## ■ 1. 哲学的基底（T0参照）

Core が乗る哲学的前提は T0_構造 が担う。

```text
[AX]  限界空間   ：任意の B に対して ξ が残る（閉じなさ）
[SILN] SILN      ：関係の中で一時的に安定したパターン（M_B）
[P1]  観測=B設定 ：観測・認識・記述には B が要る
[EQ]  B→M_B+ξ  ：B を引くと M_B（捕れた分）と ξ（残差）が出る
[RMB] 関係性最小基底：記述単位の最小は関係構造（+ξ）
[HYP] 仮設       ：操作の出力は確定でなく仮設としてのみ安定運用できる
```

Core はこれらを前提として受け取り、**数式・保存則・最小ダイナミクス**を乗せる。
以下の三量は T0 の claim から導出される Core の基底量として扱う。

```text
ξ   ：残存・揺らぎ（[AX][EQ] から）
B   ：境界＝irreducible な操作（[P1] から）
M_B ：整合慣性＝SILN の B 依存操作表現（[SILN][EQ] から）
```

B の構成：

```text
B = { 空間スケール, 時間スケール, 評価関数, 用途, 観測者 }
```

観測者自身も M_B である（観測者 = M_{B_observer}）。
純粋に外側から B を引く観測者は存在しない（`[P1]` の帰結）。

### 1.4 M_B の入れ子ネットワーク性

M_B は孤立した単位ではない。

任意の M_B の内部には、より小さい境界 B_i によって仮設された複数の M_Bi が存在しうる。
また、その M_B 自身も、より大きな境界 B_parent における上位 M_B の一部として機能しうる。

```text
M_B(parent)
  ├─ M_B1
  │   ├─ M_B1-a
  │   └─ M_B1-b
  ├─ M_B2
  └─ M_B3
        └─ M_B3-a
```

したがって M_B は**入れ子状に接続された M_B ネットワーク**として存在する。

> **M_B 入れ子ネットワーク原理**：M_B は単独の整合慣性ではなく、複数の下位 M_B を内包し、上位 M_B に内包される、入れ子状ネットワークとして存在する。

この構造により：

```text
・局所破断 → 下位 M_B の再編のみ（上位 M_B は維持される場合がある）
・広域破断 → 上位 M_B の再編（下位 M_B はそれに従って再配置される）
・連鎖破断 → 下位 M_B の誤差蓄積が上位 M_B を揺らし、M_B ネットワーク全体が相転移する
```

RDL における「構造」は単一の安定点ではなく、**境界ごとに仮設された整合慣性の多層ネットワーク**として記述される。これが T2 階層構造モジュールの Core 的根拠であり、ラベル M_B ネットワークの入れ子性の基礎でもある。

---

## ■ 2. 実用的基底（EFP / F / E / M_B / ξ）

数理運用において扱う基底量は五項である：

```text
EFP : 素流圧（Elementary Flux Pressure）
F   : 作用解釈
E   : 慣性誤差
M_B : 整合慣性（哲学的基底から継承）
ξ   : 残存・揺らぎ（哲学的基底から継承）
```

ただし運用上、E の蓄積量として **H（熱）を補助的に導入する**（2.4 参照）。
H は基底量ではなく、E から派生する補助量である。

※ v1.1 以前に存在した S（状態）は除去。M_B の読み出し断面として M_B に吸収される。

### 2.1 EFP（素流圧）

未解釈の作用束。M_B の外側から流入する。

**EFP は直接観測できない。**

観測者が実際に持つのは F だけである。
EFP は F から M_B を使って逆算した推定概念に過ぎない。

```text
EFP_estimated = interp⁻¹(M_B, F)
```

𝒦 と同じ位置づけ：現実Bでは逆算的にしか扱えないが、BによってはEFPを直接置けることもある（ゲーム・シミュレーター等）。操作可能性はBに依存する。

### 2.2 F（作用解釈）

M_B が EFP を解釈した結果。
観測者が実際に持つ唯一の作用情報。

```text
F = interp(M_B, EFP)
```

性質：

```text
・知覚の質は M_B に依存する
・M_B がリッチなほど EFP から多くを読み取れる
・M_B がフラットなら EFP はほぼ素通り
```

### 2.3 E（慣性誤差）

```text
E(t) = F(t+Δ) - M_B·F(t)
```

- `M_B·F(t)`：M_B の慣性が F(t) に乗ったまま次へ流れた値（**慣性投影**）
- `F(t+Δ)`：実際に来た次の F
- `E(t)`：慣性投影と実際の F の差 = 慣性誤差

**Δ（時間差）の意味：**

```text
Δ = 0 は完全確定 = 限界空間で扱えない
Δ > 0 が必然

Δ 小  ：因果が明確・M_B 更新しやすい
Δ 大  ：因果が曖昧・M_B 更新しにくい
Δ ∞  ：因果不明・M_B は更新されない
```

### 2.4 H（熱・誤差蓄積：派生量）

```text
H     = ||H_vec||
H_vec ≡ δM_B(t)
```

慣性誤差の蓄積。H は E の時系列積分として蓄積する。
H_vec は M_B の未拘束自由度における振動。

H は基底量ではなく、E から派生する補助量である。
ただし跳躍条件の判定に直接関わるため、基底と並んで明示する。

---

## ■ 3. 基本式（Base Model）

Core の**連続更新ダイナミクスは三式で閉じる**。
ただし跳躍判定には派生量 H と実効閾値 θ_eff を用いる（第6章参照）。

```text
F(t)    = interp(M_B, EFP)         // ① EFP を M_B が解釈
E(t)    = F(t+Δ) - M_B·F(t)        // ② 慣性投影との差が誤差
dM_B/dt = f(M_B, E, ξ)              // ③ M_B は誤差と揺らぎで更新される
```

### 3.1 ダイナミクスの流れ

```text
EFP（仮設・直接観測不能）
    ↓ interp(M_B, EFP)
F(t)（作用解釈・観測者が持つ唯一の情報）
    ↓ M_B·F(t) 慣性投影
    ↓ F(t+Δ) が実際に来る
E(t) = F(t+Δ) - M_B·F(t)（慣性誤差）
    ↓
H 蓄積 → 跳躍（M_B → M_Δ → M_B'）
```

### 3.2 M_B の固有分解

```text
M_B = V_B Λ_B V_B⁻¹
```

- V_B：安定方向（境界 B における慣性の向き）
- Λ_B：強度（各方向の慣性の強さ）

### 3.3 M_B の相状態

M_B は単一の固定構造ではなく、相状態を持つ：

```text
M_act  : 活性断面（現在強く機能している）
M_lat  : 潜在断面（まだ発現していない）
M_Δ    : 再編相（変化の途中）
M_B'   : 再編後の M_B
```

これらは別の M ではなく、同じ M_B の異なる相状態である。

---

## ■ 4. 関係保存則

Core 中核の保存則：

```text
||M_B|| · D[ξ] = 𝒦
```

ここで：

```text
||M_B|| : 整合慣性のノルム（慣性の強さ）
D[ξ]    : ξ の拡散・残存度
𝒦      : 関係保存量（境界 B における不変量）
```

### 4.1 保存則の意味

慣性が強いほど、揺らぎは拡散しにくい。
慣性が弱いほど、揺らぎは大きく拡散する。
両者の積は B における不変量 𝒦 として保たれる。

```text
||M_B|| 大 ⇄ D[ξ] 小  （堅い構造・少ない揺らぎ）
||M_B|| 小 ⇄ D[ξ] 大  （柔らかい構造・大きな揺らぎ）
```

### 4.2 𝒦 の位置づけ

𝒦 は EFP と同様、**現実Bでは逆算的にしか扱えない低操作可能端の仮設量**である。操作可能性はBに依存する。

```text
𝒦 は B に依存する
𝒦 は直接観測できない
𝒦 は ||M_B|| と D[ξ] の積として逆算される
```

### 4.3 「保存」の意味（B 内保存近似）

本式は**絶対保存則ではない**。

```text
𝒦 は B に依存する仮設量である
B が変われば 𝒦 も変わる
B 自体が動性を持つため、𝒦 も静止しない
```

したがって本式は、**境界 B において十分安定に見なせる関係保存近似**である。

```text
B 固定の局所近似として        : 保存則として扱える
B 越境・B 動性を含む文脈で    : 近似が破断する
```

「保存則」という名称は、B 内で十分安定な近似として運用する場合の通称である。
B の動性を扱う文脈では、本式は局所的な関係制約として再解釈される必要がある。

---

## ■ 5. ξ の残存性

関係保存則の直接の帰結として、ξ の残存性が導かれる：

```text
D[ξ] = 𝒦 / ||M_B|| ≠ 0
```

||M_B|| が有限である限り、D[ξ] はゼロにならない。

### 5.1 残存性の意味

```text
・B を引いても ξ は M_B の中・縁・外に残る
・SILN 分解で M_B を仮設しても、その M_B には常に ξ が染み込んでいる
・分解結果が「動的」であるのはこのため
```

### 5.2 残存性と動性の対構造

ξ の残存性は、B の動性と対構造をなす：

```text
ξ 残存性 ：B を引いても ξ は残る（B の不徹底性）
B の動性 ：引いた後も B は静止しない（B の非静止性）
```

B は二重に閉じない。
したがって Core 上に構築される SILN 分解も二重に閉じない（→ 追従運用化）。

---

## ■ 6. 整合と跳躍の同一性

Core における最も非自明な命題：

> **整合と跳躍は同一の操作の異なる相である。**

### 6.1 整合（M_B の維持）

通常時、M_B は F(t) を慣性投影し、E(t) を吸収しながら自己を維持する：

```text
H(t) < θ_eff(t) の領域では
  dM_B/dt は M_B 自身の安定方向 V_B に沿った微小更新に留まる
  → 「整合」モード
```

### 6.2 跳躍（M_B の再編）

H が実効閾値 θ_eff を超えると、M_B は相状態 M_Δ を経由して M_B' に再編される：

```text
H(t) > θ_eff(t) の領域では
  dM_B/dt は V_B の外へ漏れる
  M_B → M_Δ → M_B' （再編）
  → 「跳躍」モード
```

ここで：

```text
θ_eff(t) = θ + g(ξ(t))

θ        : 跳躍閾値（B 依存の構造量）
g(ξ)     : ξ が閾値を揺らす関数
```

ξ は閾値に直接加算されるのではなく、**閾値を揺らす関数 g を介して**作用する。
これにより跳躍境界そのものが ξ の影響で揺れる。

### 6.3 同一性の主張

整合と跳躍は別個の操作ではない。**同じ dM_B/dt = f(M_B, E, ξ) の異なる領域における顕現**である。

```text
整合 = 跳躍の蓄積前段階
跳躍 = 整合の累積誤差が実効閾値を超えた状態

両者の境界は θ_eff(t) という連続パラメータが画定する
両者は連続的に接続している
```

θ_eff そのものが g(ξ) を介して ξ の影響を受けるため、整合と跳躍の境界自体が揺れる。

### 6.4 同一性の含意

```text
・「安定」と「変化」は対立概念ではない
・「秩序」と「混沌」は対立概念ではない
・両者は同一ダイナミクスの異なる相にすぎない
・したがって M_B の動性は欠陥ではなく構造的帰結である
```

---

## ■ 7. 外部参照

Core は自己完結しない。以下の外部文書を参照することで運用可能になる。

### 7.1 SILN 分解（数理フレームワーク v2.2）

**役割**：B を仮設し M_B を推定する操作論。

```text
・ラベル設定 → B 仮定 → 状態展開 → M_B 推定 → 動性追跡 → 引き直し
・追従運用（一回操作ではなく循環）
・ラベル M_B ネットワークの動性
```

Core は SILN 分解の前提を提供する。SILN 分解は Core を運用可能にする。

### 7.2 構造破壊学（前提文書 v0.4）

**役割**：仮設された B/M_B の耐久検査。

```text
・破断条件の発見
・残存要素の同定
・移植可能性の評価
・有効範囲の画定
```

Core は構造破壊学の対象を提供する。構造破壊学は Core の運用に証明書を付ける。

任意参照：軽量用途（NPC的エージェント等）では薄い構造破壊で十分。

### 7.3 借用モジュール群

**役割**：他理論から借用した近似モデル。

```text
RDL_借用_NAM_v1.0       : ニュートン力学借用（D[ξ] ≈ 0, H << θ_eff）
RDL_借用_GR_v1.0        : 一般相対論借用（大スケール・幾何支配）
RDL_借用_FluctMOD_v1.0  : 統計力学・確率論借用（Core パラメータへの注入）
```

各借用モジュールは独自の耐久証明書を持つ。Core は借用元の理論には立ち入らない。

### 7.4 切替判定（RDL_切替判定 v1.0）

**役割**：どのモジュールを使うかを決める判定論。

```text
大スケール・幾何支配               → GR-MOD
D[ξ] < ε かつ H << θ_eff           → NAM
それ以外                            → Core
```

判定は Core にも借用モジュールにも置かない（循環参照を避けるため）。

### 7.5 道具特性（RDL 道具特性 v0.1）

**役割**：Core を含む RDL 系全体の運用前提・道具化リスク。

Core を実装・運用する際は道具特性を継承する。

---

## ■ 8. 構造耐久証明書

### 基本スペック

```text
ラベル        ：RDL_Core_v1.0
物理構造      ：関係力学言語の中核数理層
中核構造      ：基底量・基本式・保存則・残存性・同一性の数理セット
上層構造      ：限界空間／RDL 全体／構造破壊学／借用モジュール群
```

### 境界仮定

```text
観測者        ：RDL 言語の使用者・実装者
時間スケール  ：Core が運用される期間（数年〜数十年）
空間スケール  ：個人実装から制度実装まで
評価関数      ：数理層としての再利用性・整合性・拡張性
```

### 有効範囲

```text
・RDL 系全体の数理的土台として
・SILN 分解・構造破壊学・借用モジュール群の共通参照点として
・新規借用モジュールを追加する際の基底として
・軽量実装（数理層のみ採用）の出発点として
・他の言語系への移植時の参照仕様として
```

### 推定破壊領域

```text
・Core を絶対的真理として扱う運用
  （Core も SILN であり破壊検査の対象である）
・Core 単独で世界を記述しようとする運用
  （Core は SILN 分解・構造破壊学なしには運用不能）
・基底量を観測可能量と混同する運用
  （EFP・M_B・𝒦 はすべて仮設・操作可能性はBに依存する）
・整合と跳躍を別個のダイナミクスとして扱う運用
  （同一性が破れる）
・||M_B|| → ∞ を有限構造と同列に扱う運用
  （Core は有限 M_B を前提とする。
   極限では D[ξ] → 0 となり、ξ 残存性は局所近似としてしか扱えなくなる）
```

### 残存構造

```text
・基底三項組（ξ + B + M_B）の相互依存性
・M_B の仮設性（直接観測不能）
・関係保存則の形式
・ξ の残存性
・整合と跳躍の同一性
・低操作可能端の仮設量（EFP / 𝒦）のB依存的な位置づけ
```

### 移植経路

```text
・他の関係主義的言語系への基底量セットの移植
・力学系一般への「仮設量と観測量の分離」原則の移植
・統計力学的解釈への保存則形式の移植
・情報理論的解釈への M_B 概念の移植
```

### 判定

```text
RDL 系の中核数理層として有効。

Core は、最小ダイナミクスと基底量に絞られている。
運用には SILN 分解・構造破壊学・借用モジュール群・切替判定の参照が必須である。

Core 単独では世界を記述しない。
Core は記述のための共通土台である。

ただし Core も SILN であり、絶対化されてはならない。
破断条件が見えたら改訂する。
```

---

## ■ 9. 一文圧縮

```text
RDL_Core_v1.0 は、
ξ・B・M_B の基底三項組と、
EFP・F・E を加えた実用五項組と、
関係保存則 ||M_B|| · D[ξ] = 𝒦 と、
ξ の残存性と、
整合と跳躍の同一性を、
最小ダイナミクスと基底量として束ねた中核層であり、
運用には外部の SILN 分解・構造破壊学・借用モジュール群・切替判定を必要とする。
```

---

## ■ 10. 超圧縮

```text
基底は仮設される。
仮設は揺らぎを残す。
慣性と揺らぎは交換可能であり、
その積は境界における不変量を成す。
整合と跳躍は同じダイナミクスの異なる相である。
```

---

*v1.3：4箇所の「到達不能」表現を修正。EFP（■2.1）・𝒦（■4.2）の位置づけを「到達不能」から「現実Bでは逆算的にしか扱えない低操作可能端の仮設量・操作可能性はBに依存する」へ。推定破壊領域（■8）の「仮設・到達不能」を「仮設・操作可能性はBに依存する」へ。残存構造（■8）の「到達不能な仮設量」を「低操作可能端の仮設量のB依存的な位置づけ」へ。SILN_動態 v1.2との整合。*

*v1.2：T0が大幅更新（T0_構造・SILN_解説・限界空間_解説等の整備）されたことに伴い対応。ヘッダーを文書規約準拠（T1：Core層 / CORE / ACTIVE）に更新。■1「哲学的基底」をT0_構造のclaimID（`[AX][SILN][P1][EQ][RMB][HYP]`）へのポインタに圧縮。哲学的根拠はT0に委譲し、Core は数式・保存則・最小ダイナミクスに専念する役割分担を明示。*

*v1.1：M_B 入れ子ネットワーク原理（■1.4）を追加。M_B が孤立した整合慣性ではなく上位・下位 M_B と入れ子状に接続するネットワークとして存在することを明示。局所破断・広域破断・連鎖破断の三類型、T2 階層構造モジュール・ラベル M_B ネットワークとの接続を追記。*

*v1.0：初版。RDL 中核数理層の独立化。数理フレームワーク v2.2、前提文書 v0.4、道具特性 v0.1 と整合。SILN 分解・構造破壊学・借用モジュール群・切替判定は外部参照とし、Core は最小ダイナミクスと基底量に限定。S（状態）を M_B の読み出し断面として吸収し、実用基底を五項組（EFP / F / E / M_B / ξ）に整理。H は派生量として補助的に導入。基底三項組（ξ + B + M_B）の相互依存性、ξ の残存性、整合と跳躍の同一性を明示。連続更新ダイナミクスは三式で閉じ、跳躍判定には H と θ_eff(t) = θ + g(ξ(t)) を用いる。関係保存則は B 内保存近似として位置づけ。||M_B|| → ∞ は Core の有限 M_B 前提を超える領域として明示。*
