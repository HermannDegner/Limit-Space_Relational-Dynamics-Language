# 関係力学言語とは如何なるものか――構造抽出版

*T0：哲学的基底層 / CORE / DRAFT v0.1*  
*依存：（T0暗黙前提）*  
*構造抽出元：関係力学言語とは如何なるものか v0.3*  
*生成規約：RDL_構造圧縮プロトコル v0.1*  

---

## ■ 0. B_compress

```text
B_compress :=
  「『関係力学言語とは如何なるものか』の
    文書全体を、再構成・比較・翻訳・破壊検査に使える
    構造IRとして抽出する」
```

本稿は原文の代替ではない。
原文の文章・比喩・リズム・節ごとの説明を保持することではなく、
**RDLそのものを定義している主要関係を失わないこと**を目的とする。

```text
Source = 関係力学言語とは如何なるものか v0.3
IR = 本稿

Goal:
  Invariant(Source) ≈ Invariant(Reconstruct(IR))

Not required:
  Source = Reconstruct(IR)
```

---

## ■ 1. source-role

```text
source-role :=
  RDLの哲学的入口
  +
  RDL自身の自己位置づけ
  +
  日常語 ↔ 圧縮記号 の橋
```

原文は、RDLを単一理論として閉じるのではなく、

```text
RDL := ⟨見, 述, 操⟩(関係)
```

という操作言語として導入する窓口文書である。

---

## ■ 2. 根

```text
[AX]
B → M_B + ξ
∀B : ξ ≠ 0
B_max ∉ 観測者

[SILN]
pattern = survival-biased(局所線形 ⊂ 非線形)

[EQ]
‖M_B‖ · D[ξ] = 𝒦

{[AX], [SILN]} = 相補二根
```

本稿全体は、主に `[AX]` を認識・記述・運用へ展開し、
`[SILN]` を「関係の中で一時的に保たれる構造」の側から補完する。

---

## ■ 3. 主要ラベル

```text
RDL
  = 関係を見て、記述し、操作するための言語

B
  = 観測・記述・操作の境界

M_B
  = B内で一時的に安定して扱われる構造

ξ
  = Bを引いても残る未確定・取りこぼし

F
  = 観測者に現れ、保持される作用

SILN
  = 関係の中で一時的に安定するパターン

硬度
  = 同Bにおける再現性・作用結合の強さとして測る安定度

[GATE]
  = 外れたとき世界ではなく自己側を修正する継続条件
```

---

## ■ 4. 主要命題

### P1：RDLは主義ではない

```text
主義 = M_B(基点固定)
主義 ∈ 観測対象
∴ RDL ∉ int(主義)
```

RDLは特定の立場を禁止しない。
立場そのものを観測対象へ戻すため、特定主義の内部に固定されない。

---

### P2：RDLは閉じた理論ではない

```text
理論 = M_B(dom 有限)
理論 ∈ 観測対象
∴ ¬closed(RDL)
```

理論は利用対象であり、比較・破壊・再述の対象でもある。

---

### P3：RDLは言語として働く

```text
言語 := { 見, 分, 接, 再述, 再操 }
```

RDLの中心は「正しい世界像を一つ与える」ことではなく、
関係を扱い直せる操作可能性にある。

---

### P4：観測者は世界そのものを保持しない

```text
観測者 ⊁ 世界
hold = F のみ
```

したがってRDLの直接対象は、世界そのものではなく、
観測者に現れた作用・記述・想像を含む `F空間` である。

```text
dom(RDL)
=
F空間
=
関与可能全域{
  現前可能,
  想像可能,
  記述可能
}
```

---

### P5：真偽・実在虚構を入口で固定しない

```text
{ 実在, 虚構, 真, 偽 } ∉ 入力

{ 実在らしさ,
  虚構らしさ,
  真らしさ,
  偽らしさ } ∈ 出力
```

入口で分類せず、観測後の硬度差として扱う。

---

### P6：現実は高硬度な関係として扱う

```text
現実 ≠ 実在
現実 = SILN(硬度 → max)

∇硬度 ≠ 0
→ ¬相対主義
```

すべてを等価化するのではなく、再現性・作用結合の勾配を保持する。

```text
硬度 < ∞
→ M_B → M_Δ → M_B'
```

したがって、現実は頼れるが更新不能ではない。

---

### P7：RDLは「見る・記述する・操作する」で働く

```text
見 := { 支え, 重み, 偏り, 破断点 }

述 := 関係構造 + ξ
      かつ追従的

操 := ΔB
      | 破壊検査
      | 残存抽出
      | 移植
```

記述は固定ではなく追従であり、操作には境界の引き直しも含む。

---

### P8：RDLは既知理論へ接続するが還元されない

```text
RDL : 接続 ∧ ¬還元
```

複数の時代・分野で似た破断型が現れることを、
RDLは独立収束として読む。

```text
既知群
=
独立収束(Δ担体 ∧ ΔB → 同破断型)
```

ただし、個別比較は別文書へ委ねられる。

---

### P9：万能記述性には迷信化リスクがある

```text
¬presort
→ ∀x : SILN-describable

risk:
  ∀translatable ≈ content(∅)
```

「何にでも当てられる」は長所であると同時に、
何も反証しない空理論へ落ちる危険でもある。

その継続条件が `[GATE]` である。

```text
[GATE]
=
外れ → 自己訂正
≡ 仮設運用

¬[GATE]
→ [MET]
```

---

### P10：RDLは最終判定を出さない

```text
measure(硬度) = B
→ ξ 残

最終判定 = ∅
出力 = ∇硬度(可引き直し)
```

RDLは真理確定装置ではなく、
より細かく観測・比較するためのレンズとして位置づけられる。

---

### P11：RDL自身も固定されない

```text
RDL ∈ SILN

RDL ← Δ{
  反例,
  誤読,
  応用,
  接続
}

¬完成(RDL)
```

RDL自身も文書化・言語化によるBの内部にあり、ξを免れない。

```text
[DOC]: self-eat
RDL = B(多次元 → 線形)
→ ξ(RDL) ≠ 0
```

---

## ■ 5. 主線

```text
世界を一つに確定できない
        ↓
Bを引いて扱う
        ↓
B → M_B + ξ
        ↓
観測者が保持するのはFのみ
        ↓
真偽・実在虚構を入口で固定しない
        ↓
同じ観測機械を異なる対象へ適用
        ↓
硬度の勾配を出力する
        ↓
見・述・操で関係を扱う
        ↓
外れたら自分側を修正する
        ↓
最終判定を閉じない
        ↓
RDL自身にも同じ操作を返す
```

最短に圧縮すると、

```text
RDL
=
言語(
  F空間の対象へ
  同機械⟨見,述,操⟩を適用し、
  硬度差を出力し、
  ξを残し、
  外れれば自己修正し続ける
)
```

---

## ■ 6. 主要関係

```text
RDL ≠ 主義
RDL ≠ 閉理論
RDL = 言語

観測者 ≠ 世界そのもの
観測者 holds F only

真偽・実在虚構 ≠ 入力前提
真らしさ・実在らしさ = 出力候補

現実 ≠ 絶対実在
現実 ≈ 高硬度SILN

相対性 ≠ 相対主義

記述 ≠ 完成
記述 = 追従

接続 ≠ 還元

万能記述性
↔ 迷信化リスク

RDLの道具性
↔ [GATE]継続

RDL ∈ SILN
RDL ∉ 外部視点
```

---

## ■ 7. ξ_source

原文自身が保持している未確定は、少なくとも次である。

```text
ξ_source := {
  Bを引いた後にも残る取りこぼし,
  硬度測定自体に含まれるB依存性,
  世界そのものとFの対応の最終確定不能性,
  既知理論との接続範囲,
  RDLの普遍性の上限,
  RDL自身の文書化による損失,
  RDLの将来更新方向
}
```

原文はこれらを除去対象とはせず、
RDLを閉じないための構造として保持する。

---

## ■ 8. ξ_compress

本構造抽出によって新たに生じた未確定は次である。

```text
ξ_compress := {
  原文の音・リズム・比喩の損失,
  「現実」「硬さ」の説明的ニュアンスの縮退,
  節間の語りの温度差の消失,
  日常語と圧縮記号を併置する教育的作用の損失,
  独立収束についての説明強度の簡略化,
  [GATE]を「ただ一つの支え」と表現する修辞的強度の消失
}
```

したがって本稿だけから、原文の文章的作用を完全再構成することは要求しない。

---

## ■ 9. 破断条件

本構造抽出版の読みは、少なくとも次で破断する。

```text
B1:
原文の主要命題が更新され、
P1〜P11のいずれかが撤回・再定義された場合

B2:
B → M_B + ξ
または
hold = F only
がRDL基底から除去された場合

B3:
「硬度」が間主観的再現性とは異なる基底量へ
再定義された場合

B4:
[GATE]が道具性の継続条件ではなくなった場合

B5:
RDLが自己食的構造を持たない体系へ変更された場合
```

その場合、Sourceを再圧縮する。

---

## ■ 10. 残存構造

個別の式・比喩・語彙が変更されても、現時点で比較的残りやすい骨格は次である。

```text
何かを見るには境界が要る

境界を引けば取りこぼしが残る

観測者は世界そのものではなく
自分に現れた作用を扱う

対象を先に真偽分類せず
同じ観測操作へ乗せる

差は後から硬度として出す

モデルは使うが絶対化しない

外れたらモデル・境界・言語側を修正する

RDL自身もこの操作から免れない
```

---

## ■ 11. 翻訳・再構成時に保存すべき不変量

```text
I1. RDLは主義ではない
I2. RDLは閉じた単一理論ではない
I3. RDLは関係を扱う言語である
I4. B → M_B + ξ を消さない
I5. 観測者は世界そのものではなくFを保持する
I6. 真偽・実在虚構を入力前提にしない
I7. 硬度には勾配があり、相対主義へ落とさない
I8. 記述は追従であり、操作にはΔBを含む
I9. 万能記述性と迷信化リスクを同時に保持する
I10. [GATE] = 外れたら自己修正する
I11. 最終判定を閉じない
I12. RDL自身もSILNであり、自己適用される
```

翻訳・簡易化・再構成で上記のいずれかが消えた場合、
原文と同じRDL像を保持しているとはみなさない。

---

## ■ 12. 最短IR

```text
ROOT:
  B → M_B + ξ
  ∀B : ξ ≠ 0
  B_max ∉ 観測者
  hold = F only

RDL:
  ≠ ideology
  ≠ closed theory
  = language for relations

OPERATION:
  see
  → describe
  → operate
  → test break
  → redraw B if necessary

INPUT POLICY:
  { true, false, real, fictional } ∉ presort

OUTPUT:
  hardness gradient
  + ξ

GUARD:
  failure → self-correction

META:
  RDL ∈ SILN
  RDL ≠ external viewpoint
  ¬complete(RDL)
```

---

## ■ 13. 原文との関係

```text
原文
= 本文
  + 節ごとの圧縮記号

本稿
= 文書全体の構造IR
```

したがって両者は競合しない。

```text
原文
  → 読解・導入・響き・説明

構造抽出版
  → 比較・翻訳・破壊検査・再構成
```

同じRDLを、異なるBで切り出した対文書として扱う。

---

*v0.1：『関係力学言語とは如何なるものか』v0.3を、RDL_構造圧縮プロトコル v0.1 に基づいて全文書レベルの構造IRへ変換。節ごとの圧縮記号とは別に、B・主要命題・関係・主線・ξ_source / ξ_compress・破断条件・残存構造・翻訳不変量を抽出した。*
